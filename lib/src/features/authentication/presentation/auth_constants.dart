const accessToken = 'x-auth-token';
const refreshToken = 'x-auth-refresh-token';

const formContentSizeMobile = 80.0;
const formContentSizeTab = 80.0;

const active = 'ACTIVE';
const pending = 'PENDING';

const errorSendingOtp = 'Error sending OTP';
const newAndOldNotSame = 'New password and old password are not same';
const validOtp = 'Enter a valid OTP';
const accountActivationTitleMsg = 'Welcome to Axlerate, 🙏🏼';
const accountActivationSemiTitle = 'the future of logistics.';
const accountActivationSubTitle =
    'You\'re almost done creating your account.\n Please fill in the boxes below complete the process.';
